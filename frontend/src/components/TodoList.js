import React from "react";
import Todo from "./Todo";

const TodoList = () => {
  return (
    <div>
      {/* {filterTodos.map((todo) => (
        <Todo />
      ))} */}
    </div>
  );
};

export default TodoList;
